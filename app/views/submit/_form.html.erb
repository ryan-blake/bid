<!-- <%= form_for [submit] do |f| %>
<% if submit.errors.any? %>
# #6
<div class="alert alert-danger">
  # #7
  <h4><%= pluralize(submit.errors.count, "error") %>.</h4>
  <ul>
    <% submit.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
# #8
<%= form_group_tag(submit.errors[:title]) do %>
<%= f.label :title %>

<%= f.number_field :price, class: 'form-control', placeholder: "Enter price" %>
<% end %>



<div class="form-group">
  <%= f.submit "Save", class: 'btn btn-success' %>
</div>
<% end %> -->
