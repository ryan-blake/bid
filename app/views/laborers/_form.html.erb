<%= form_for(@laborer) do |f| %>
  <% if @laborer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@laborer.errors.count, "error") %> prohibited this laborer from being saved:</h2>

      <ul>
      <% @laborer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
