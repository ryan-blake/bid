
<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-8">
    <% @jobs.each do |job| %>
    <div class="media">
      <div class="media-body">
        <h4 class="media-heading">
          <% unless job.selected_submission_id %>
          <%= link_to job.title, job %>
        </h4>
        <small>
          <%= job.description %>
        </small>
        <small>
          <td><%= link_to 'Show', job %></td>
        </small>
      </div>
    </div>
    <% end %>
    <% end %>
  </div>
  <body>
<% if current_laborer %>

<%= link_to "Premium Account", new_charge_path, class: 'btn btn-success' %>
<% end %>


<br>
  <% if current_client %>
    <%= link_to 'New Job', new_job_path %>
  <% end %>
</br>
